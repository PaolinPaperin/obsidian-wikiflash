# Per-user plugin settings — never publish these
data.json

# Local Claude Code project notes (contains local machine paths) — keep private
CLAUDE.md
AGENTS.md

# macOS
.DS_Store

# Editor / tooling
node_modules/
*.log
.vscode/

# Local disposable demo vault for screenshots / GIF recording
WikiFlash Demo Vault/
